import { StyleSheet } from "react-native";

export default (ActionButtonStyles = StyleSheet.create({
  actionText: {
    color: "#fff",
    fontSize: 16,
    textAlign: "center"
  },
  action: {
    backgroundColor: "#24CE84",
    borderColor: "transparent",
    borderWidth: 1,
    paddingLeft: 16,
    paddingTop: 14,
    paddingBottom: 16
  }
}));
